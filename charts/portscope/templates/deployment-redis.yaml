apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "portscope.fullname" . }}-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "portscope.fullname" . }}-redis
  template:
    metadata:
      labels:
        app: {{ include "portscope.fullname" . }}-redis
    spec:
      containers:
        - name: redis
          image: "{{ .Values.redis.repository }}:{{ .Values.redis.tag }}"
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "portscope.fullname" . }}-redis
spec:
  ports:
    - port: 6379
  selector:
    app: {{ include "portscope.fullname" . }}-redis
